<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
	<properties resource="jdbc_mysql.properties" />
	<!-- 给参数取别名 -->
	<typeAliases>
		<!-- <typeAlias type="com.zjy.beans.Student" alias="Student"/> -->
		<package name="com.zjy.beans"/>
	</typeAliases>
	<!-- 配置运行环境 -->
	<environments default="mysqlEnvironment">
		<environment id="mysqlEnvironment">
			<transactionManager type="JDBC"/><!-- 采用JDBC管理,未来spring -->
			<dataSource type="POOLED"><!-- 数据库连接池技术 -->
				<property name="driver" value="${jdbc.driver}"/>
				<property name="url" value="${jdbc.url}"/>
				<property name="username" value="${jdbc.username}"/>
				<property name="password" value="${jdbc.password}"/>
			</dataSource>
		</environment>
	</environments>
	<!-- 注册映射文件 -->
	<mappers>
        <mapper resource="com/zjy/dao/mapper.xml"/>
    </mappers>
	<!-- 
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
   -->
</configuration>